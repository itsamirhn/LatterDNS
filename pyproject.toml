[project]
name = "latterdns"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "click>=8.3.1",
    "dnspython>=2.8.0",
]

[dependency-groups]
dev = [
    "ruff>=0.14.6",
]
